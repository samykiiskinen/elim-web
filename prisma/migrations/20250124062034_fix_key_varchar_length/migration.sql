BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Key] ALTER COLUMN [key] VARCHAR(10) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
